# Benchmark evidence

This directory contains model-specific reports retained from the repository
history.

Report filenames should identify the workflow or model generation. Exact
runtime, iteration count, displacement error, and terminal-step statements are
evidence for that benchmark only.

Do not use benchmark reports as the canonical installation or API guide.
